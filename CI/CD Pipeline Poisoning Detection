(metadata.event_type = "PROCESS_LAUNCH" AND (strings.contains(principal.process.file.full_path, "gitlab-runner") OR strings.contains(principal.process.file.full_path, "github-actions") OR strings.contains(principal.process.file.full_path, "jenkins"))) OR (metadata.event_type = "FILE_MODIFICATION" AND (strings.contains(target.file.full_path, "/workspace/") OR strings.contains(target.file.full_path, "/.github/") OR strings.contains(target.file.full_path, "/.gitlab/"))) OR (metadata.event_type = "NETWORK_CONNECTION" AND (strings.contains(principal.process.file.full_path, "jenkins") OR strings.contains(principal.process.file.full_path, "gitlab-runner") OR strings.contains(principal.process.file.full_path, "github-actions")) AND (target.port = 22 OR target.port = 2375 OR target.port = 443))
