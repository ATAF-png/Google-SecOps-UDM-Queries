metadata.event_type = "NETWORK_DNS" and principal.process.file.full_path = /gup.exe/ and network.dns.questions.name != "notepad-plus-plus.org" and network.dns.questions.name != /github.com/
 
metadata.event_type = "PROCESS_LAUNCH" and principal.process.file.full_path = /gup.exe/ and target.process.file.full_path != /explorer.exe/ and target.process.file.full_path != /npp.*Installer/ 
