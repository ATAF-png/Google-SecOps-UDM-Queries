metadata.log_type = "GCP_CLOUDAUDIT"
re.regex(target.resource.name, `ci-cd|build|jenkins`)
target.resource.resource_type = "SERVICE_ACCOUNT"
principal.user.userid != ""
security_result.action = "ALLOW"
